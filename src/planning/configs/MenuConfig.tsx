import { MENU_PLANNING_KEY, MENU_PLANNING_URL } from '../constants/menu';

const menuConfig = {
  menu_key: MENU_PLANNING_KEY,
  config: {
    // title: <FormattedMessage id="new-mail" />,
    title: 'Board',
    url: `${MENU_PLANNING_URL}`,
    icon: <i></i>,
    color: '#4383b4'
  }
};
export default menuConfig;
