// material-ui

import { Spin } from 'antd';

// loader style

// ==============================|| Loader ||============================== //

const Loader = () => {
  return (
    <div className="app-loader">
      <Spin spinning />
    </div>
  );
};

export default Loader;
