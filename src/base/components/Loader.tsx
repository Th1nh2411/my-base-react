// material-ui

// loader style

// ==============================|| Loader ||============================== //

const Loader = () => {
  return (
    <div
      style={{
        position: 'fixed',
        top: 0,
        left: 0,
        zIndex: 1400,
        width: '100%'
      }}
    >
      ...
    </div>
  );
};

export default Loader;
