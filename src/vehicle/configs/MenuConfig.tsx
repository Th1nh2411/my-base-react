
import { MENU_VEHICLE_KEY, MENU_VEHICLE_URL } from '../constants/menu';

const menuConfig = {
  menu_key: MENU_VEHICLE_KEY,
  config: {
    // title: <FormattedMessage id="new-mail" />,
    title: 'Board',
    url: `${MENU_VEHICLE_URL}`,
    icon: <i></i>,
    color: '#4383b4'
  }
};
export default menuConfig;
